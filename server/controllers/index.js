const hackathonController = require('./hackathons');
const teamController = require('./teams');
const userController = require('./users');

module.exports = {
  hackathonController,
  teamController,
  userController,
};
